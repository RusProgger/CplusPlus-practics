#include <iostream>
#include <format>
#include <string>

template <typename T>
T print(const T& value) {
    return value;
}

int main() {

    std::cout << "Enter word: ";
    

    auto result = print(value);


    return 0;
}